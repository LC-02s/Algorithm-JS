// https://www.acmicpc.net/problem/1152 (단어의 개수)

/*
const fs = require('fs');
const input = fs.readFileSync('/dev/stdin').toString().split('\n');

// trim : 문자열 양 끝의 공백을 제거함
const arr = input[0].trim().split(' ');

if (arr == '') console.log(0);
else console.log(arr.length);
*/